\begin{figure}[!b]
\centering
\includegraphics[width=0.95\linewidth]{figs/fig_perf_by_length.pdf}
\caption{Per-token AUROC on LongFact as the input prefix length $T$
grows. ACT-ViT with the default pooled token axis
(N\textsubscript{eff}=6) collapses past $T = 48$ and drops below
chance at $T \ge 192$; widening the pool to N\textsubscript{eff}=186
partially rescues long prefixes but does not match Temp-ViT's flat
profile.}
\label{fig:perf_by_length}
\end{figure}
