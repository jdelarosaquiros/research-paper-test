\begin{figure}[!b]
\centering
\includegraphics[width=0.95\linewidth]{figs/fig_signal_propagation.pdf}
\caption{Per-offset ROC-AUC of a linear probe on RAGTruth
(Llama-2-13b). Teal bars: tokens in the non-hallucinated context
adjacent to the span; coral (F, M, L): first/middle/last token of the
hallucinated span. The signal ramps from chance at offset $-24$ to
AUC $\approx 0.66$ at offset $-1$, rises monotonically through the
span ($0.71 \to 0.79$), and decays slowly afterwards --- motivating
window-based localization (Section~\ref{sec:non_static}).}
\label{fig:signal_propagation}
\end{figure}
