\begin{figure}[!b]
\centering
\includegraphics[width=0.95\linewidth]{figs/fig_inference_time.pdf}
\caption{Wall-clock to score every token of a $T = 384$ LongFact
passage on one H200 (batch~1, FP32). ACT-ViT needs $T$ sequential
forwards (one per token); Temp-ViT produces all per-token logits in a
single pass, $\approx 25\times$ faster.}
\label{fig:inference_time}
\end{figure}
